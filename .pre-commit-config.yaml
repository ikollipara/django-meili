repos:
  - repo: local
    hooks:
      - id: uv-lock-check
        name: uv lock --check
        entry: uv lock --check
        language: system
        files: "^(pyproject.toml|uv.lock)$"
        pass_filenames: false
